<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Tema escuro do Poupai -->
    <style name="Theme.Poupai" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Cores principais -->
        <item name="colorPrimary">@color/Primary_Blue</item>
        <item name="colorPrimaryVariant">@color/Primary_Blue</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Cores secundárias -->
        <item name="colorSecondary">@color/Color_gold</item>
        <item name="colorSecondaryVariant">@color/Color_gold</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Fundo escuro -->
        <item name="android:windowBackground">#0F172A</item> <!-- tom escuro azulado -->
        <item name="android:statusBarColor">@color/Primary_Blue</item>
    </style>
</resources>
